# 🐋 Whalez-AI Mobile Mirror

A React Native + Expo app that mirrors the web Command Console and runtime dashboard.
Connects to your backend at http://localhost:5050 to:
- View live runtime health logs
- Send intents and system commands
- Display agent status

## Run locally
```bash
cd app/mobile
npm install
npm start
```
