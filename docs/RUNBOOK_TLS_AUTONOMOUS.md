- Purpose: local CA + signed server certs for previews/internal.
- One command (Linux): scripts/linux/start_secure.sh
- One command (Windows): scripts/windows/start_secure.bat
- CI: tls-preflight job ensures keys/certs parse and exist.
- Artifacts path: .secrets/tls/
- Rotate: delete server.* and rerun start script; CA persists.
